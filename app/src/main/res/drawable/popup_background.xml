<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#E8FFFFFF" /> <!-- Background color -->
    <corners android:radius="20dp" />              <!-- Rounded corners -->
    <stroke
        android:width="1dp"
        android:color="#DCDCDC" />  <!-- Optional border -->
    <padding
        android:bottom="8dp"
        android:left="8dp"
        android:right="8dp"
        android:top="8dp" />  <!-- Padding inside -->
</shape>
